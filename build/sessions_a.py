SESSIONS_A = [

# =====================================================================
{
 "num": 1,
 "slug": "session01_printing_names_input",
 "title": "Printing, names, and input",
 "subtitle": "Your first six lines, and the one trap in them",
 "mon": "Mon Sep 14",
 "fri": "Fri Sep 18",
 "new": ["print", "a variable: a name for a value", "reassignment", "input", "type()", "int()", "f-string", "reading a NameError"],
 "reuse": None,
 "reuse_note": "First session. Nothing to retrieve yet, so the ten minutes go to machine setup and one shared rule: read it, trace it, then run it.",
 "opener": {
   "title": "Setup and the house rule",
   "minutes": 10,
   "body": "Open the editor and make a file called <code>Jonathan_session01.py</code>, with your own first name in place of Jonathan. Every file you make this term starts with your first name, then an underscore, then the session. Write your name in a comment on line 1 as well. Then, before anyone runs anything, the rule for the whole semester: you say out loud what a line will do before you press run. Being wrong out loud is the point. It is how you find out what you actually believe.",
   "questions": [],
 },
 "chunks": [
  {"title": "One line, one job",
   "teach": "A program is a list of instructions. Python does them top to bottom, one at a time, and then stops. <code>print</code> puts words on the terminal.",
   "code": 'print("Robofun terminal is live.")',
   "run": "py",
   "predict": "The quote marks are in the code. Will they be in the output?",
   "reveal": "No. The quotes tell Python where the text starts and stops. They are punctuation for Python, not part of your message."},

  {"title": "The misfire, on purpose",
   "teach": "Now break it. Type <code>pritn</code> instead of <code>print</code> and run it. You are going to read errors all semester, so you may as well meet one you built yourself.",
   "code": 'pritn("Robofun terminal is live.")',
   "run": "py",
   "predict": "Python is going to complain. Which word will it complain about?",
   "reveal": "It names <code>pritn</code>, not the message. Python does not know that word. Read the last line of an error first. It tells you the category and the word involved."},

  {"title": "A name for a value",
   "teach": "The equals sign does not mean equals here. It means <em>put this value under this name</em>. After that line, the name works anywhere you would have written the value. A name that holds a value is called a <b>variable</b>. That is the word every Python book uses, so this course uses it too: <code>player</code> is a variable, and its value right now is Ada.",
   "code": 'player = "Ada"\nprint(player)\nprint("player")',
   "run": "py",
   "predict": "Two print lines, one with quotes and one without. Do they print the same thing?",
   "reveal": "No. Without quotes, Python looks up the name and finds Ada. With quotes, it just prints the six letters you typed. This is the most common first-week mix-up in the room."},

  {"title": "Reassignment reads right to left",
   "teach": "A name can be pointed at a new value. When the old name appears on both sides, work out the right side first, then store the answer back under the name.",
   "code": 'score = 10\nprint(score)\nscore = score + 5\nprint(score)',
   "run": "py",
   "predict": "Line 3 has score on both sides. What does the second print show?",
   "reveal": "15. Python computes <code>10 + 5</code> first, then stores 15 under <code>score</code>. The old 10 is gone."},

  {"title": "input always gives you text",
   "teach": "<code>input</code> stops the program, waits for typing, and gives you what was typed. It gives you <em>text</em>, always, even when the person typed digits. Run this with 3 as the answer.",
   "code": 'age = input("Robot age in years: ")\nprint(age + 1)',
   "stdin": "3\n",
   "run": "py",
   "predict": "You typed the number 3. Does <code>age + 1</code> give you 4?",
   "reveal": "No. You get a TypeError. The 3 you typed is the text <code>\"3\"</code>, and Python will not add 1 to text. Text plus text is allowed; text plus number is not."},

  {"title": "Two kinds of value, and type() tells them apart",
   "teach": "Python keeps text and whole numbers as two different kinds of value, and it calls the kind a <em>type</em>. <code>type()</code> tells you which kind you have. <code>str</code> is short for string, which is Python's word for text. <code>int</code> is short for integer, a whole number. Quote marks make text, even around digits. Run this with 3 as the answer again.",
   "code": 'print(type("4"))\nprint(type(4))\nage = input("Robot age in years: ")\nprint(type(age))',
   "stdin": "3\n",
   "run": "py",
   "predict": "Three type() calls. Which of them say str, and which say int?",
   "reveal": "The first is str, because the quotes make it text. The second is int. The third is str as well: you typed 3, but input handed it back as text, which is why chunk 5 crashed. Text and number are different types, and a plus sign will not mix them. The word <code>class</code> in the output is Python's way of saying kind: read <code>&lt;class 'str'&gt;</code> as text and <code>&lt;class 'int'&gt;</code> as whole number."},

  {"title": "int() changes the type, on one line of its own",
   "teach": "<code>int()</code> takes text that looks like a whole number and gives back the same value as a number. It changes the type from str to int. Do the type change on its own line while you are learning. You will see it folded into one line in other people's code later.",
   "code": 'age = input("Robot age in years: ")\nage = int(age)\nprint(age + 1)',
   "stdin": "3\n",
   "run": "py",
   "predict": "Same program with one line added. What changes?",
   "reveal": "Now <code>age</code> holds the number 3 rather than the text 3, so adding 1 works. Add <code>print(type(age))</code> after line 2 and it says int. This trap comes back every time a program asks for a number, so it goes on your ledger card today."},

  {"title": "f-strings put values inside a sentence",
   "teach": "Putting an <code>f</code> in front of the opening quote lets you drop a name into the middle of a sentence using curly braces. It is shorter than gluing pieces together with plus signs, and it does not care whether the value is text or a number.",
   "code": 'player = "Ada"\nscore = 15\nprint(f"{player} has {score} points.")',
   "run": "py",
   "predict": "What lands in the braces, the name or the value?",
   "reveal": "The value. Python looks up each name inside the braces and drops the value in. Forget the <code>f</code> and you print the braces themselves, which is a good bug to see once."},
 ],
 "hinge": {"q": "Someone writes <code>total = input(\"How many tickets? \")</code> and then <code>print(total + 2)</code>. The program crashes. Name the fix in one line of code.",
           "code_a": 'total = input("How many tickets? ")\ntotal = int(total)\nprint(total + 2)',
           "a": "<code>total = int(total)</code> before the print. Or convert inside the print with <code>int(total) + 2</code>. Either answer is correct; the first is the one we write this term."},
 "brief": {
   "title": "Build: the rider card",
   "intro": "Picture the gate at a theme park roller coaster. Before anyone gets on, the person at the gate asks three things: your name, how tall you are, and how many tickets you have. Then they say it back to you, so you can check it. Your program is that person at the gate. It asks the three questions in the terminal, and the person typing the answers is the rider. The height and the tickets are numbers, and that is where the trap from chunks 5 to 7 comes back.",
   "minutes": 25,
   "steps": [
     "Ask for a rider name and store it.",
     "Ask for a height in inches. It comes back as text, so change its type to a number with <code>int()</code>, on its own line.",
     "Ask for a number of tickets, and change its type the same way.",
     "Print one f-string sentence that uses all three values.",
     "Add 1 to the ticket count and print a second sentence showing the new total.",
   ],
   "expected_got": [
     ["Line 4 prints all three values in one sentence", ""],
     ["Line 6 shows the ticket count one higher than typed", ""],
     ["No quote marks appear in the output", ""],
   ],
 },
 "exits": {
   "floor": "A program that asks for a name and prints it back inside an f-string.",
   "middle": "The full rider card: three questions, two type changes, two printed sentences.",
   "stretch": "Add a fourth question asking for a group size, then print the total tickets for the group without using a calculator, letting Python do the multiplication inside the print.",
 },
 "ledger": "Stamp 1: <code>print</code>, <code>name = value</code> makes a variable, <code>input</code> gives text, <code>type()</code>, <code>int()</code> changes the type, <code>f\"{name}\"</code>. Write the TypeError message from chunk 5 on the card in your own handwriting.",
 "teacher": {
   "timing": [
     "0:00 setup and house rule, 10 min",
     "0:10 chunks 1 to 4, 20 min (fast; most students have seen print)",
     "0:30 chunks 5 to 7, the int() trap, 15 min (slow down here)",
     "0:45 chunk 8 f-strings, 10 min",
     "0:55 build brief, 25 min",
     "1:20 exits, ledger stamp, 10 min",
   ],
   "misconceptions": [
     ["Prints the name instead of the value, or the reverse.", "Point at the quote marks and ask what they tell Python. Do not explain; make them run both lines."],
     ["Reads <code>score = score + 5</code> as an equation and objects that it cannot be true.", "Rename the operation out loud: store the right side under the name on the left. Trace it with two boxes on the whiteboard."],
     ["Writes <code>int(age)</code> but does not store it, then wonders why nothing changed.", "Run their line. The conversion happened and was thrown away. Give this a full minute of silence."],
   ],
   "standards": ["NY-5.OA.2", "NY-6.EE.2", "MP6"],
   "routing": "If the room finishes early, do not start gates. Send them to the stretch exit and let them ask for four values. Gates need a clean 90 minutes.",
 },
},

# =====================================================================
{
 "num": 2,
 "slug": "session02_comparisons_and_gates",
 "title": "Comparisons and gates",
 "subtitle": "True and False, three-way gates, and two conditions at once",
 "mon": "Mon Sep 28",
 "fri": "Fri Sep 25",
 "new": ["comparison operators", "True and False", "if / elif / else", "= against ==", "and", "or", "ask-until-valid while"],
 "reuse": "Session 1: reassignment and f-strings",
 "reuse_note": "The Monday section did not meet the prior week, so it has been two weeks since session 1. Assume the int() trap is gone and re-teach it inside the opener rather than mid-lesson.",
 "opener": {
   "title": "Retrieval: last session's trap",
   "minutes": 10,
   "body": "No notes, no editor. Answer both on paper, then open the editor and check yourself.",
   "questions": [
     {"q": "Write the two lines that ask for a height and store it as a number.",
      "a": "<code>height = input(\"Height in inches: \")</code> then <code>height = int(height)</code>."},
     {"q": "What does <code>print(\"player\")</code> show if <code>player = \"Ada\"</code>?",
      "a": "The six letters <code>player</code>. The quotes stop Python from looking up the name."},
   ],
 },
 "chunks": [
  {"title": "A comparison is a question with a yes or no answer",
   "teach": "Comparing two values gives <code>True</code> or <code>False</code>. Those are values like any other. You can print them. Every comparison sign is typed as ordinary keys: <code>==</code> is two equals signs, <code>!=</code> is an exclamation mark then an equals sign, and <code>&lt;=</code> and <code>&gt;=</code> are two keys each. There is no single slashed-equals character in Python.",
   "code": 'height = 52\nprint(height > 48)\nprint(height == 48)\nprint(height != 48)',
   "run": "py",
   "predict": "Three comparisons on the same number. How many print True?",
   "reveal": "Two. 52 is greater than 48, so the first is True. It is not equal to 48, so the second is False and the third is True. Count them before you run, every time."},

  {"title": "The three-way gate",
   "teach": "The part after <code>if</code> is called the condition. A condition is a comparison, or anything else that comes out True or False. <code>if</code> runs its block when its condition is True. <code>elif</code> gets checked only when everything above it was False. <code>else</code> catches whatever is left. Python checks top to bottom and stops at the first True.",
   "figure": '<h3>What a gate is</h3><p>A gate is an <code>if</code> line together with the indented lines under it. It has two parts. The <b>condition</b> is the test after the word <code>if</code>. It comes out True or False. The <b>block</b> is the indented lines under it. They run only when the condition is True, and are skipped when it is False. The condition decides whether the block runs.</p><pre class="anatomy">if <span class="cond">height &gt; 48</span>:                <span class="lbl lbl-c">&larr; condition</span>\n<span class="blk">    print("Cleared to ride.")</span>  <span class="lbl lbl-b">&larr; block</span>\nelif <span class="cond">height == 48</span>:             <span class="lbl lbl-c">&larr; next condition</span>\n<span class="blk">    print("Borderline. Ask staff.")</span>  <span class="lbl lbl-b">&larr; its block</span>\nelse:                            <span class="lbl">&larr; everything else</span>\n<span class="blk">    print("Not tall enough yet.")</span>  <span class="lbl lbl-b">&larr; its block</span>\n</pre><div class="legend"><span><i class="c"></i> condition: a True or False test</span><span><i class="b"></i> block: indented lines that run only when their condition is True</span></div><p>The three tests together are one gate with three ways through. Python reads it top to bottom, takes the first way whose condition is True, and skips the rest. One block runs, never two. <code>else</code> has no condition; it is the way through when every test above it was False. Think of the height gate at the coaster: one test, and you either go through or you do not.</p>',
   "code": 'height = 48\nif height > 48:\n    print("Cleared to ride.")\nelif height == 48:\n    print("Borderline. Ask staff.")\nelse:\n    print("Not tall enough yet.")',
   "run": "py",
   "predict": "Height is exactly 48. Which of the three lines prints?",
   "reveal": "The middle one. The first test is False because 48 is not greater than 48, so Python moves down to the elif. Boundary values are where gate bugs live."},

  {"title": "One equals sign is an order, two is a question",
   "teach": "This is the single most common typo in the room. Run it and read the error.",
   "code": 'height = 52\nif height = 48:\n    print("Borderline.")',
   "run": "py",
   "predict": "Does this print nothing, or does it crash?",
   "reveal": "It crashes before it runs a single line, and Python tells you what it wanted instead. One equals sign stores a value. Two equals signs ask a question."},

  {"title": "and: both must be True",
   "teach": "Real gates check more than one thing. <code>and</code> gives True only when both sides are True. Nothing is nested here; the two conditions sit side by side on one line.",
   "code": 'height = 52\nhas_ticket = True\nif height > 48 and has_ticket:\n    print("Board the coaster.")\nelse:\n    print("Step aside.")',
   "run": "py",
   "predict": "Set has_ticket to False on paper. Which line prints then?",
   "reveal": "Step aside. With <code>and</code>, one False sinks the whole condition. Change the value and rerun to confirm, one change per run."},

  {"title": "or: one is enough",
   "teach": "<code>or</code> gives True when either side is True. Read it out loud with the word or in it and it behaves the way English does.",
   "code": 'is_staff = False\nhas_pass = True\nif is_staff or has_pass:\n    print("Gate opens.")\nelse:\n    print("Gate stays shut.")',
   "run": "py",
   "predict": "Both names are on the screen. Does the gate open?",
   "reveal": "Yes. <code>has_pass</code> is True, and with <code>or</code> that is enough on its own."},

  {"title": "A while loop and its stopping condition",
   "teach": "A <code>while</code> loop has a condition too, the same kind of True or False test as an <code>if</code>. It repeats its block as long as the condition is True and stops the first time it is False. That makes it a stopping condition: read it and you know what will make the loop stop. Every while loop you write this semester has a stopping condition you can point at. This one stops when the answer is yes or no and nothing else.",
   "code": 'answer = ""\nwhile answer != "yes" and answer != "no":\n    answer = input("Ready? Type yes or no: ")\nprint("Recorded:", answer)',
   "stdin": "maybe\nsoon\nyes\n",
   "run": "py",
   "predict": "Someone types maybe, then soon, then yes. How many times does the question appear?",
   "reveal": "Three times. The condition is checked before each pass. Once the answer is yes, the stopping condition is no longer True, so the loop stops and the print runs."},
 ],
 "hinge": {"q": "From session 1: a rider types 52 at an input prompt and the program crashes on the line that says <code>if height > 48:</code>. The gate is written correctly. What is wrong?",
           "code": 'height = input("Height in inches: ")\nif height > 48:\n    print("Cleared to ride.")\nelse:\n    print("Not tall enough yet.")',
           "code_a": 'height = input("Height in inches: ")\nheight = int(height)\nif height > 48:\n    print("Cleared to ride.")\nelse:\n    print("Not tall enough yet.")',
           "a": "<code>height</code> is still text, and Python will not compare text with a number. The last line of the error says TypeError. The missing line is <code>height = int(height)</code>, before the gate. It is the same trap as session 1, inside a gate this time."},
 "brief": {
   "title": "Build: the ride operator",
   "minutes": 25,
   "steps": [
     "Ask for a height, convert it to a number.",
     "Ask whether the rider has a ticket, and keep asking until they type yes or no.",
     "Open the ride only when the height clears 48 and the ticket answer is yes.",
     "Add an elif for exactly 48 that sends them to staff.",
     "Print a different sentence for each of the three outcomes.",
   ],
   "expected_got": [
     ["Typing 52 and yes opens the ride", ""],
     ["Typing 52 and no does not open the ride", ""],
     ["Typing maybe at the ticket question asks again", ""],
     ["Typing 48 and yes sends them to staff", ""],
   ],
 },
 "exits": {
   "floor": "A two-way gate on height alone that answers correctly for 52, 48, and 40.",
   "middle": "The full operator: converted height, ask-until-valid ticket question, three outcomes.",
   "stretch": "Add a staff override so that typing staff at the ticket question opens the ride regardless of height, using <code>or</code> and no nesting.",
 },
 "ledger": "Stamp 2: the six comparisons, <code>if / elif / else</code>, <code>==</code> against <code>=</code>, <code>and</code>, <code>or</code>, and a while loop with its stopping condition circled.",
 "teacher": {
   "timing": [
     "0:00 retrieval opener, 10 min",
     "0:10 chunks 1 to 3, 20 min",
     "0:30 chunks 4 and 5, and / or, 15 min (new this term, do not rush)",
     "0:45 chunk 6 while, 12 min",
     "0:57 build brief, 23 min",
     "1:20 exits, ledger stamp, 10 min",
   ],
   "misconceptions": [
     ["Writes a second <code>if</code> where an <code>elif</code> belongs, then reports that two messages print.", "Correct behaviour, wrong design. Have them trace both versions with height = 52 and name the difference."],
     ["Uses <code>and</code> where <code>or</code> belongs because the English sentence said and.", "Read the requirement again slowly. Everyday and often means either."],
     ["Writes <code>while answer != \"yes\" or answer != \"no\"</code> and gets a loop that never ends.", "Substitute yes into both halves out loud. One half stays True forever. This is the best forty seconds in the session; let them find it."],
   ],
   "standards": ["NY-6.EE.5", "NY-6.EE.2", "MP6"],
   "routing": "Nesting stays banned. If a student reaches for an if inside an if, hand them <code>and</code>. If the room is fast, the staff override stretch absorbs fifteen minutes.",
 },
},

# =====================================================================
{
 "num": 3,
 "slug": "session03_loops_counters_lists",
 "title": "Loops, counters, and lists",
 "subtitle": "Repeat a block, keep a running total, and hold many values under one name",
 "mon": "Mon Oct 5",
 "fri": "Fri Oct 2",
 "new": ["for and range", "accumulator", "counter with a gate", "best so far", "lists", "indexing", "IndexError", "len", "sum and max as a reveal", "random.choice"],
 "reuse": "Session 2: the gate goes inside the loop",
 "reuse_note": "The gate from last week now runs once per item. That is what the counter pattern is, so the retrieval opener has to land before chunk 4.",
 "opener": {
   "title": "Retrieval: gates",
   "minutes": 10,
   "body": "Paper first. Then check in the editor.",
   "questions": [
     {"q": "Write a three-way gate on a variable called <code>temp</code>: above 90 prints hot, exactly 90 prints warm, anything else prints fine.",
      "code_a": 'if temp > 90:\n    print("hot")\nelif temp == 90:\n    print("warm")\nelse:\n    print("fine")',
      "a": "Three tests, top to bottom, each with its own indented print. Python stops at the first True."},
     {"q": "Both sides are False. What does <code>False and False</code> evaluate to, and what does <code>False or False</code> evaluate to?",
      "a": "Both evaluate to False. <code>and</code> and <code>or</code> give different answers only when the two sides disagree: <code>True and False</code> is False, <code>True or False</code> is True."},
   ],
 },
 "chunks": [
  {"title": "for and range repeat a block",
   "teach": "<code>range(5)</code> gives the numbers 0, 1, 2, 3, 4. Five numbers, starting at zero, stopping before five. The indented block runs once per number.</p><p><code>step</code> is a variable, and it is the loop that gives it its value. On the first pass <code>step</code> is 0. When the body finishes, the loop goes back to the top, gives <code>step</code> the next value, 1, and runs the body again. It keeps doing that until <code>range</code> has nothing left to hand out. So <code>step</code> is a different number every time the body runs, and you never write a line that changes it; the loop does that for you. This name is called the <b>loop variable</b>, and you can call it anything you like.",
   "figure": '<h3>The parts of a for loop</h3><pre class="anatomy">for <span class="cond">step</span> in <span class="nm">range(5)</span>:          <span class="lbl lbl-c">&larr; loop variable</span>  <span class="lbl lbl-n">&larr; the values</span>\n<span class="blk">    print(f"step {step}")</span>   <span class="lbl lbl-b">&larr; body: runs once per value</span>\n\n<span class="lbl">pass 1   step is 0   prints  step 0</span>\n<span class="lbl">pass 2   step is 1   prints  step 1</span>\n<span class="lbl">pass 3   step is 2   prints  step 2</span>\n<span class="lbl">pass 4   step is 3   prints  step 3</span>\n<span class="lbl">pass 5   step is 4   prints  step 4</span>\n<span class="lbl">no values left, so the loop ends</span></pre><div class="legend"><span><i class="c"></i> loop variable: a new value each pass</span><span><i class="n"></i> the values it works through, in order</span><span><i class="b"></i> body: the indented lines</span></div>',
   "code": 'for step in range(5):\n    print(f"step {step}")',
   "run": "py",
   "predict": "Five numbers. Does the list include 5?",
   "reveal": "No. It starts at 0 and stops before 5. Counting from zero is going to matter again in ten minutes when we index a list."},

  {"title": "The accumulator",
   "teach": "A running total needs a name that exists before the loop starts, and a reassignment inside the loop. The name has to start at 0, outside the loop, or there is nothing to add to. Two variables change here, in two different ways: the loop changes <code>n</code> for you on every pass, and your own line <code>total = total + n</code> changes <code>total</code>.",
   "code": 'total = 0\nfor n in range(1, 6):\n    total = total + n\nprint(f"total: {total}")',
   "run": "py",
   "predict": "range(1, 6) gives which numbers, and what do they add up to?",
   "reveal": "1 through 5, and they add to 15. Two arguments to range means start here, stop before there."},

  {"title": "A list holds many values under one name",
   "teach": "A <b>list</b> is one value that holds many values in order. Square brackets make one, with commas between the values. Store it under a name the same way as any other value: <code>riders = [...]</code> makes <code>riders</code> a variable whose value is the whole list. Each value inside the list is called an <b>item</b>. To pull one item out, write the name and then a number in brackets. That number is the item's <b>index</b>: its position in the list, counting from 0, not from 1. So the first item has index 0, and the last item of a three-item list has index 2. The word slot is used on these pages to mean the same thing as index.",
   "code": 'riders = ["Ada", "Grace", "Alan"]\nprint(riders[0])\nprint(riders[2])\nprint(len(riders))',
   "run": "py",
   "predict": "<code>len</code> says 3. So what is the biggest number you can put in the brackets?",
   "reveal": "2. Three slots, numbered 0, 1, 2. The length and the last index are always one apart, which is the source of the next error you are about to write on purpose."},

  {"title": "IndexError, on purpose",
   "teach": "Ask for a slot that does not exist. Read the error, and note that the number it names is the one you asked for.",
   "code": 'riders = ["Ada", "Grace", "Alan"]\nprint(riders[3])',
   "run": "py",
   "predict": "There are three riders. Does <code>riders[3]</code> give you Alan or an error?",
   "reveal": "An error. Alan is at slot 2. Off-by-one errors are so common that Python has a dedicated name for this one."},

  {"title": "Counter with a gate, and best so far",
   "teach": "A <code>for</code> loop can work through a list as well as a <code>range</code>. <code>for h in heights</code> gives <code>h</code> each item of the list in turn: 52 on the first pass, 47 on the second, and so on to 58. Nothing is counted; the loop gives you the items themselves.</p><p>Two patterns, one loop. The counter adds 1 only when the gate is True. Best so far keeps a name for the largest value seen and replaces it whenever something bigger turns up. Both names start before the loop.",
   "code": 'heights = [52, 47, 61, 44, 58]\ncleared = 0\ntallest = 0\nfor h in heights:\n    if h > 48:\n        cleared = cleared + 1\n    if h > tallest:\n        tallest = h\nprint(f"cleared: {cleared}")\nprint(f"tallest: {tallest}")',
   "run": "py",
   "predict": "Trace it on paper. What are the two numbers?",
   "reveal": "3 cleared and 61 tallest. Note that <code>tallest</code> changes twice on the way through: to 52, then to 61, and never again."},

  {"title": "The reveal: three built-in tools",
   "teach": "Python already has these. You wrote them by hand first so you know what they do, which means you can now read a line that uses them without guessing.",
   "code": 'heights = [52, 47, 61, 44, 58]\nprint(sum(heights))\nprint(max(heights))\nprint(min(heights))\nprint(len(heights))',
   "run": "py",
   "predict": "Which of your two hand-written patterns does <code>max</code> replace?",
   "reveal": "Best so far. <code>sum</code> replaces the accumulator. Neither replaces the counter with a gate, because that one needs your condition. This is how the rest of Python's library works too: it covers the common cases and leaves the specific ones to you."},

  {"title": "random.choice picks one for you",
   "teach": "<code>import random</code> brings in code someone else wrote. <code>random.choice</code> picks one item from a list. <code>random.seed</code> makes the picking repeatable, which is how you test a program that is supposed to be unpredictable.",
   "code": 'import random\nrandom.seed(7)\nriders = ["Ada", "Grace", "Alan"]\nprint(random.choice(riders))\nprint(random.choice(riders))',
   "run": "py",
   "predict": "The seed is fixed. If you run this twice, do you get the same two names?",
   "reveal": "Yes, the same two every time, in the same order. Remove the seed line and you get a fresh pair on every run. Seeds are a testing tool, not a game feature."},
 ],
 "hinge": {"q": "From chunk 2 today: this student wrote <code>total = 0</code> <em>inside</em> the loop instead of above it. The program runs, no error, wrong answer. What does it print for the heights list, and why?",
           "code": 'heights = [52, 47, 61, 44, 58]\nfor h in heights:\n    total = 0\n    total = total + h\nprint(f"total: {total}")',
           "run": "py",
           "code_a": 'heights = [52, 47, 61, 44, 58]\ntotal = 0\nfor h in heights:\n    total = total + h\nprint(f"total: {total}")',
           "a": "58, the last height. Resetting to 0 each pass throws away everything before it, so the final answer is just the last item added to zero. An error that does not crash is worse than one that does. This is the silent bug we spend all of session 7 on."},
 "brief": {
   "title": "Build: the day's ride log",
   "minutes": 25,
   "steps": [
     "Start with a list of at least eight rider heights.",
     "Count how many cleared 48 inches, using a counter with a gate.",
     "Find the tallest and the shortest by hand, using best so far twice.",
     "Print a summary with an f-string.",
     "Then add four lines that check your four answers using <code>sum</code>, <code>max</code>, <code>min</code>, and <code>len</code>.",
   ],
   "expected_got": [
     ["Hand-written tallest matches max()", ""],
     ["Hand-written count is not reproducible with sum() alone", ""],
     ["Summary sentence names all four numbers", ""],
     ["Changing one height in the list changes the count", ""],
   ],
 },
 "exits": {
   "floor": "A loop over a list that prints every height on its own line.",
   "middle": "The ride log: counter, two best-so-far names, f-string summary, four built-in checks.",
   "stretch": "Add an average. You have <code>sum</code> and <code>len</code>, and division with a single slash gives a decimal.",
 },
 "ledger": "Stamp 3: <code>for i in range(n)</code>, the accumulator, the counter with a gate, best so far, <code>list[0]</code> is the first, <code>len</code>, <code>sum</code>, <code>max</code>, <code>min</code>, <code>random.choice</code>, <code>random.seed</code>.",
 "teacher": {
   "timing": [
     "0:00 retrieval opener, 10 min",
     "0:10 chunks 1 and 2, 15 min",
     "0:25 chunks 3 and 4, lists and IndexError, 15 min",
     "0:40 chunk 5, the two patterns, 15 min (the core of the session)",
     "0:55 chunks 6 and 7, 12 min",
     "1:07 build brief, 18 min (note: many will not reach the built-in checks)",
     "1:25 exits, ledger stamp, 5 min",
   ],
   "misconceptions": [
     ["Starts the accumulator inside the loop.", "Do not correct it. Have them print the total inside the loop and watch it fail to grow."],
     ["Sets <code>tallest = 100</code> to be safe, then nothing ever beats it.", "Ask what value is guaranteed to lose. Starting at 0 works for heights; it fails for temperatures below zero, so say so now."],
     ["Believes <code>len</code> gives the last index.", "Print both. One is 3, one errors. Sixty seconds."],
   ],
   "standards": ["NY-5.OA.3", "NY-6.EE.2", "MP8"],
   "routing": "If time runs short, cut chunk 7 and the random material entirely. It reappears in session 8 with the cipher wheel, where it has a job to do.",
 },
},

# =====================================================================
{
 "num": 4,
 "slug": "session04_functions_that_return",
 "title": "Functions that return a value",
 "subtitle": "The line that lets one function hand an answer to another",
 "mon": "Mon Oct 19",
 "fri": "Fri Oct 9",
 "new": ["def with no parameters", "parameters", "return", "the value None", "a function calling a function", "several returns behind flat gates"],
 "reuse": "Session 3: the accumulator becomes a function you can reuse",
 "reuse_note": "This is the highest-value session in the semester. Everything downstream, including dictionaries in session 9 and the grid in session 11, gets easier once return exists.",
 "opener": {
   "title": "Retrieval: two patterns from two weeks back",
   "minutes": 10,
   "body": "Paper. No editor until both are written.",
   "questions": [
     {"q": "Write the accumulator that adds up a list called <code>scores</code>. Four lines.",
      "a": "<code>total = 0</code>, then <code>for s in scores:</code>, then <code>total = total + s</code>, then <code>print(total)</code>."},
     {"q": "Where does <code>total = 0</code> go, and what happens if it goes one line lower?",
      "a": "Above the loop. One line lower puts it inside the loop, which resets it every pass and prints the last item instead of the sum."},
   ],
 },
 "chunks": [
  {"title": "Naming a block",
   "teach": "A <b>function</b> is a block of lines with a name. <code>def</code> makes one: the word <code>def</code>, then the name you choose, then parentheses, then a colon, and the block indented underneath. Writing the name with parentheses after it, anywhere below, runs the block. That is called <b>calling</b> the function.</p><p>Where a <code>def</code> goes: at the top of the file, after any imports and before any line that does work. Python reads the file from the top, so it has to meet the <code>def</code> before it meets a call. When a program has several functions, they all sit together in that top section. This is new, and it is the half of today that has no trap in it.",
   "code": 'def greet():\n    print("Welcome to the park.")\n\ngreet()\ngreet()',
   "run": "py",
   "predict": "How many lines of output, and does the def line itself print anything?",
   "reveal": "Two lines. Defining a function does not run it. The block sits there until something calls it."},

  {"title": "Parameters let the caller fill in a blank",
   "teach": "A name inside the parentheses on the <code>def</code> line is a <b>parameter</b>: a blank that gets filled in each time the function is called. The value you put in the parentheses when you call it is the <b>argument</b>. Inside the body, the parameter behaves like any other variable.",
   "figure": '<h3>The parts of a function</h3><pre class="anatomy">def <span class="nm">greet</span>(<span class="cond">name</span>):                <span class="lbl lbl-n">&larr; name</span>  <span class="lbl lbl-c">&larr; parameter</span>\n<span class="blk">    print(f"Welcome, {name}.")</span>   <span class="lbl lbl-b">&larr; body</span>\n\n<span class="nm">greet</span>(<span class="arg">"Ada"</span>)                     <span class="lbl lbl-a">&larr; a call, with the argument "Ada"</span>\n</pre><div class="legend"><span><i class="n"></i> name: what you write to call it</span><span><i class="c"></i> parameter: the blank inside the parentheses on the def line</span><span><i class="b"></i> body: the indented lines that run when it is called</span><span><i class="a"></i> argument: the value you pass in when you call it</span></div><p>The parameter and the argument go together. <code>name</code> is the parameter, written once on the <code>def</code> line. <code>"Ada"</code> and <code>"Grace"</code> are arguments, one per call. Each time the function is called, the argument becomes the value of the parameter for that run of the body.</p>',
   "code": 'def greet(name):\n    print(f"Welcome, {name}.")\n\ngreet("Ada")\ngreet("Grace")',
   "run": "py",
   "predict": "Where does the value of <code>name</code> come from?",
   "reveal": "From the call. It is different on each of the two calls, and it does not exist outside the function at all."},

  {"title": "return hands a value back to the caller",
   "teach": "This is the new idea. <code>return</code> stops the function and hands a value back to whoever called it. The call itself becomes that value, which means you can store it, print it, or do arithmetic with it.",
   "code": 'def double(n):\n    return n * 2\n\nanswer = double(21)\nprint(answer)\nprint(double(5) + double(5))',
   "run": "py",
   "predict": "Line 6 calls double twice and adds the results. Does that work?",
   "reveal": "Yes. 42 and 20. Each call turns into its value, then the plus sign adds two ordinary numbers. A function that prints cannot do this, which is the next chunk."},

  {"title": "print and return are not the same thing",
   "teach": "Two functions that look alike. One prints, one returns. Store the result of each and look at what you get.",
   "code": 'def double_return(n):\n    return n * 2\n\ndef double_print(n):\n    print(n * 2)\n\na = double_return(21)\nb = double_print(21)\nprint(f"a is {a}")\nprint(f"b is {b}")',
   "run": "py",
   "predict": "Both functions do the same arithmetic. Will <code>a</code> and <code>b</code> hold the same value?",
   "reveal": "No. <code>a</code> holds 42. <code>b</code> holds <code>None</code>, which is Python's word for no value at all. A function with no return returns None, every time. A function like <code>double_print</code> shows you a number and gives you nothing, which is why its output can be looked at but never used."},

  {"title": "One function calling another",
   "teach": "Once a function returns a value, another function can use it. Build small pieces and stack them. This is what people mean when they say a program is made of parts.",
   "code": 'def area(w, h):\n    return w * h\n\ndef floor_cost(w, h):\n    return area(w, h) * 12\n\nprint(area(3, 4))\nprint(floor_cost(3, 4))',
   "run": "py",
   "predict": "<code>floor_cost</code> never multiplies width by height itself. Does it still work?",
   "reveal": "Yes. 12 and 144. <code>floor_cost</code> asks <code>area</code> for a number and then multiplies it by 12. Neither function depends on what the other one does inside."},

  {"title": "Several returns, gates still flat",
   "teach": "A function can have more than one <code>return</code>. The first one reached wins and the function stops there, so a gate with three exits needs no else at the end.",
   "code": 'def fare(age):\n    if age < 5:\n        return 0\n    if age < 13:\n        return 8\n    return 12\n\nprint(fare(3))\nprint(fare(10))\nprint(fare(30))',
   "run": "py",
   "predict": "<code>fare(10)</code> passes the second gate. Why does it not also reach the last line and come back as 12?",
   "reveal": "Because <code>return</code> ends the function immediately. Nothing below it runs. 0, 8, 12. Three gates, no nesting, no else."},

  {"title": "Your accumulator, as a tool",
   "teach": "Last session's four lines, wrapped in a name. Now it works on any list, and the answer comes back as a number you can use.",
   "code": 'def total_of(numbers):\n    total = 0\n    for n in numbers:\n        total = total + n\n    return total\n\nprint(total_of([52, 47, 61]))\nprint(total_of([1, 2, 3, 4]))\nprint(total_of([52, 47, 61]) + total_of([1, 2, 3, 4]))',
   "run": "py",
   "predict": "Where does <code>return total</code> have to sit, inside the loop or after it?",
   "reveal": "After it, lined up with the <code>for</code>. Put it inside the loop and the function returns on the first pass with only the first number, which is a silent bug that gives you 52 instead of 160."},
 ],
 "hinge": {"q": "From session 2: write a function called <code>can_ride</code> that takes a height and a ticket answer and returns True or False. Use <code>and</code>. Then write the one line that uses it in a gate.",
           "code_a": 'def can_ride(height, ticket):\n    return height > 48 and ticket == "yes"\n\nif can_ride(52, "yes"):\n    print("Board the coaster.")',
           "a": "<code>def can_ride(height, ticket):</code> then <code>return height &gt; 48 and ticket == \"yes\"</code>. Used as <code>if can_ride(52, \"yes\"):</code>. Note that the comparison itself is already True or False, so it can be returned directly with no gate inside the function."},
 "brief": {
   "title": "Build: three tools and one program that uses them",
   "minutes": 25,
   "steps": [
     "Write <code>total_of(numbers)</code> that returns a sum.",
     "Write <code>tallest_of(numbers)</code> that returns the largest value, using best so far.",
     "Write <code>average_of(numbers)</code> that returns the mean, and make it call <code>total_of</code> rather than adding again.",
     "Make a list of heights and print all three results in one f-string.",
     "Check every one of your three tools against <code>sum</code>, <code>max</code>, and your own arithmetic.",
   ],
   "expected_got": [
     ["total_of matches sum()", ""],
     ["tallest_of matches max()", ""],
     ["average_of calls total_of rather than looping again", ""],
     ["Each function returns rather than prints", ""],
   ],
 },
 "exits": {
   "floor": "<code>double(n)</code> written, called, and its result stored in a name and printed.",
   "middle": "All three tools returning correctly, plus the f-string summary.",
   "stretch": "Write <code>count_over(numbers, limit)</code> that returns how many items beat the limit. Two parameters, one counter, one return. This is the pattern the built-in library does not cover for you.",
 },
 "ledger": "Stamp 4: <code>def name(parameter):</code>, <code>return</code> hands a value back, no return means <code>None</code>, a function may call another function, several returns are fine with flat gates. Write the None line from chunk 4 on the card.",
 "teacher": {
   "timing": [
     "0:00 retrieval opener, 10 min",
     "0:10 chunks 1 and 2, 12 min (new, but there is no trap in them; keep it moving)",
     "0:22 chunk 3, return, 15 min",
     "0:37 chunk 4, print against return, 15 min (the most important chunk of the semester; do not compress)",
     "0:52 chunks 5 and 6, 15 min",
     "1:07 chunk 7, 8 min",
     "1:15 build brief, 10 min (most rooms get one tool done; two is the middle exit)",
     "1:25 exits, ledger stamp, 5 min",
   ],
   "misconceptions": [
     ["Writes <code>return</code> and <code>print</code> on consecutive lines and cannot say which one the caller sees.", "Delete the print. Ask where the number went. Then put it back and ask who it was for."],
     ["Calls the function but does not store or print the result, then says nothing happened.", "Correct. The value came back and nobody caught it. Compare to <code>int(age)</code> with no assignment from session 1."],
     ["Puts <code>return</code> inside the loop in chunk 7.", "It returns 52 instead of 160. Trace one pass out loud. The indentation is the bug."],
     ["Assumes a parameter name and an outside variable with the same name are the same thing.", "Name the parameter something absurd and show the function still works. Do this once, in front of everyone."],
   ],
   "standards": ["NY-8.F.1", "NY-6.EE.9", "MP7"],
   "routing": "If half the room is still on chunk 4 at the hour mark, stop there and make chunk 4 the whole session. Sessions 5 and 7 can absorb a week of slip; session 9 cannot be taught without this.",
 },
},

]
